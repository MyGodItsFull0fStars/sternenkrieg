<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rebelartstudios.sternenkrieg">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Needed for Google Nearby Connections -->
        <meta-data
            android:name="com.google.android.gms.nearby.connection.SERVICE_ID"
            android:value="@string/service_id" />

        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
            <activity
            android:name="com.example.rebelartstudios.sternenkrieg.Spielen"
            android:label="@string/app_name" />
        -->
        <activity
            android:name=".Modi"
            android:label="@string/app_name" />
        <activity
            android:name=".Dice"
            android:label="@string/app_name" />
        <activity
            android:name=".EndScreen"
            android:label="@string/app_name" />
        <activity
            android:name=".Map"
            android:label="@string/app_name" />
        <activity
            android:name=".Networking"
            android:label="@string/app_name" />
        <activity
            android:name=".Spielfeld"
            android:label="SK_UI3" />
        <activity android:name=".Options" />
        <activity android:name=".About" />
        <activity android:name=".Socket_main" />
        <activity android:name=".Network.Host" />
        <activity android:name=".Network.Client1" />
        <activity android:name=".QR_Reader" />

        <service
            android:name=".PlayAudio"
            android:enabled="true" />

        <activity android:name=".HighScore" />
        <activity android:name=".PowerUp"></activity>
    </application>

</manifest>